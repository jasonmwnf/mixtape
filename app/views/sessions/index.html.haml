- @bands.all.each do |band|
  = link_to band.name, band
  = "| #{band.bio} | #{band.genre}"
  %hr

/ = form_tag("/", method: "post") do
/   = select_tag "genres", options_for_select(['Metal', 'Acoustic'])
/   = submit_tag("Create New MixTape")